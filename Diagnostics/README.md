# Diagnostics

Temporary folder for transferring log files, migration reports, and error
samples between the Windows VM and the Linux dev environment via GitHub.

These files are working artifacts — not part of the application source.
